<section class="card">
  <a href="/character/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{creator}}</p>
    </div>
  </a>
</section>